name: Bad CI/CD Workflow

on:
  push: # Запускается, когда есть push в любую ветку
    branches:
      - master # Запускается только для ветки master

jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Run bad practice step
        run: echo "This is a bad practice"
